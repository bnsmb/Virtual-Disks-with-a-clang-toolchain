[http]
	sslVerify = true
	sslCAinfo = /etc/security/ca-certificates.crt

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true

